[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "meadows-server"
dynamic = ["version"]
description = "MEADOWS coordination hub: object-oriented Hub, single chokepoint emit, JWT auth, JSONL persistence."
readme = "README.md"
requires-python = ">=3.14"
license-expression = "MIT"
authors = [{ name = "MEADOWS" }]
dependencies = [
    "meadows-protocol",
    "meadows-jsonlogic",
    "diskcache>=5.6.0",
    "python-socketio>=5.11.0",
    "uvicorn>=0.30.0",
    "pyjwt>=2.12.0",
    "endow>=0.1.6",
]

[project.optional-dependencies]
dev = ["hatch", "pytest>=8.0.0", "pytest-asyncio>=0.23.0", "pytest-cov>=5.0.0", "ruff==0.14.9", "invoke>=2.2.0"]
integration = [
    "meadows-client",
    "meadows-bot",
    "aiohttp>=3.9.0",
    "httpx>=0.27.0",
]

[tool.hatch.version]
path = "src/meadows/server/__about__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/meadows"]

[tool.uv.sources]
meadows-protocol = { path = "../meadows-protocol", editable = true }
meadows-jsonlogic = { path = "../meadows-jsonlogic", editable = true }
meadows-client = { path = "../meadows-client", editable = true }
meadows-bot = { path = "../meadows-bot", editable = true }

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.ruff.lint]
select = ["F", "E", "W", "Q", "A", "SIM", "ARG", "PTH", "RUF", "C90", "N", "YTT"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
